CC = gcc
CFLAGS = -std=c99 -g -Wall -Wextra -pedantic -O2 -ggdb
LIBS = -lm

NAME = 
OBJECTS =

all: $(NAME)

%.o: %.c Makefile
	$(CC) $(CFLAGS) -c $<

$(NAME): $(NAME).o $(OBJECTS)
	$(CC) $(CFLAGS) -o $@ $^ $(LIBS)

run: $(NAME)
	./$(NAME)

clean:
	rm *.o
# cursor: 5 del
